export interface AcademicProject {
    name: string;
    imgPath: string ;
    startDate: string;
    endDate: string;
    description: string;
    sourceUrl: string;
    previewUrl: string;
    featured?: boolean;
}